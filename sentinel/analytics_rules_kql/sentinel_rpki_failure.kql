RedLanternLogs_CL
| where event_type_s == "rpki_validation"
| where validation_state_s == "invalid"
| summarize Count=count() by prefix_s, origin_asn_s
| extend Severity = case(prefix_s startswith "203.0.113.", "Critical", "Medium")
